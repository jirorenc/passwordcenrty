function entegrasyon(s,r,x,j,k_id){
    var mSoap=s;
    var mRest=r;
    var mXml=x;
    var mJson=j;
    if(mJson!=0){ alert("json seçildi") }
    if(mSoap!=0){ alert("Soap seçildi") }
    if(mXml!=0){ alert("XML seçildi") }
    if(mRest!=0){ alert("rest seçildi") }

}